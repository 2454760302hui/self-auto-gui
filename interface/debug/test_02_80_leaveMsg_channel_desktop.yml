config:
  name: 在线_v6
#  parameters: #卡迪尔 参数
#    x: "${get_ksid}"


teststeps:
-
  name: 开启留言-自有渠道设置，方式为留言转离线，留言内容按桌面渠道
  request:
    method: post
    headers:
      temp-id: ${get_temp_id}
    url: /text/chat-set/chat/config/updateRestLeaveMsgConfigById/4

    data:
      configId: 903e189ab6ab4155b911984198743780 #固定值
      leaveToOfflineConfigId: ${get_leave_desktop_id}
      msgFlag: 0
      msgToTicketFlag: 2
      leaveType: 1
      msgLeaveTxt: ${fake.text(max_nb_chars=100)}
      msgLeaveContentTxt: ${fake.text(max_nb_chars=100)}
      channelType: 0
      channelFlag: 1
#  extract:
#    get_leave_id: $..id
#    get_ticketDeployName: $..ticketDeployName
#  export:
#    - get_leave_id
#    - get_ticketDeployName

  validate:

    - eq: [ $..retCode,'000000' ]
#    - eq: [$..id,'${get_leave_id}' ]
#    - eq: [ $..ticketDeployId, '${get_ticketDeployId}']
#    - eq: [ $..ticketDeployName,'默认插件' ]
#    - eq: [ $.items.0.fieldName,"客户标签"]
#    - eq: [ $..iframeId,'${get_iframeId}']



