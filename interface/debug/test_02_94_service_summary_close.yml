config:
  name: 在线_v6
#  parameters: #卡迪尔 参数
#    x: "${get_ksid}"


teststeps:
-
  name: 服务总结-关闭分类
  request:
    method: post
    headers:
      temp-id: ${get_temp_id}
    url: /text/basic-config-service/updateChatUnitInfo/4

    data:
      unitId: ${get_unitId}
      unitName: ${get_typeName}
      unitStatus: 0
      unitDoc: fake.text(max_nb_chars=1000)
#  extract:
#    get_unitId: $..unitId
#  export:
#    - get_unitId


  validate:

    - eq: [ $..retCode,'000000' ]
#    - eq: [ $..unitId, '${get_unitId}']

