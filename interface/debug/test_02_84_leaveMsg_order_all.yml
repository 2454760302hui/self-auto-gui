config:
  name: 在线_v6
#  parameters: #卡迪尔 参数
#    x: "${get_ksid}"


teststeps:
-
  name: 开启留言-自有渠道设置，方式为留言转工单，整体设置-勾选客服回复工单对客户可见
  request:
    method: post
    headers:
      temp-id: ${get_temp_id}
    url: /text/chat-set/chat/config/updateRestLeaveMsgConfigById/4

    data:
      configId: 903e189ab6ab4155b911984198743780 #固定值
      leaveToTicketConfigId: 4389
      msgFlag: 0
      msgToTicketFlag: 1
      leaveType: 0
      ticketDeployId: ${get_ticketDeployId_id}
      ticketShowFlag: 1
      ticketDeployName: 默认插件
  #  extract:
#    get_leave_id: $..id
#    get_ticketDeployName: $..ticketDeployName
#  export:
#    - get_leave_id
#    - get_ticketDeployName

  validate:

    - eq: [ $..retCode,'000000' ]
#    - eq: [$..id,'${get_leave_id}' ]
#    - eq: [ $..ticketDeployId, '${get_ticketDeployId}']
#    - eq: [ $..ticketDeployName,'默认插件' ]
#    - eq: [ $.items.0.fieldName,"客户标签"]
#    - eq: [ $..iframeId,'${get_iframeId}']



