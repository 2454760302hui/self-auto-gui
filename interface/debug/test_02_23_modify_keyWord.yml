config:
  name: 在线_v6
#  parameters: #卡迪尔 参数
#    x: [ "4","2","1","3" ] #人工优先，仅人工，仅机器人,机器人优先


teststeps:
-
  name: 新版console-修改关键词
#  skip: 不清楚ksId 从何来-添加时没有返回-修改和删除是必填项-转人工列表也没返回
  request:
    method: POST
    url: /text/chat-set/keyword/updateKeyword/5
    headers:
      temp-id: ${get_temp_id}
    data:
      robotFlag: '1'
      keyword: 'test'
      matchFlag: '1'
      queueFlag: '1'
      groupId: '[ ]'
      transferFlag: '3'
      transferTips: ${fake.text()}
      ksId: ${get_ksid}

#  extract:
#    ksId: body.data.ksId
#
#  export:
##    - keyword
#    - ksId

  validate:
    - eq: [ $..retMsg, '操作成功']


