config:
  name: 在线_v6
#  parameters: #卡迪尔 参数
#    x: "${get_ksid}"


teststeps:
-
  name: 开启留言-自有渠道设置，方式为留言转工单，按渠道设置
  request:
    method: post
    headers:
      temp-id: ${get_temp_id}
    url: /text/chat-set/chat/config/updateRestLeaveMsgConfigById/4

    data:
      configId: 903e189ab6ab4155b911984198743780 #固定值
      leaveToTicketConfigId: ${get_4order_id_desktop}
      msgFlag: 0
      msgToTicketFlag: 1
      leaveType: 1
      ticketShowFlag: 1
      ticketDeployId: ${get_ticketDeployId_id}
      channelType: 0
      channelFlag: 1
      ticketDeployName: 默认插件
      agentId:
  #  extract:
#    get_leave_id: $..id
#    get_ticketDeployName: $..ticketDeployName
#  export:
#    - get_leave_id
#    - get_ticketDeployName

  validate:

    - eq: [ $..retCode,'000000' ]



