config:
  name: 在线_v6
#  parameters: #卡迪尔 参数
#    x: "${get_ksid}"


teststeps:
-
  name: 获取留言id
  request:
    method: get
    headers:
      temp-id: ${get_temp_id}
    url: /text/chat-set/chat/config/getTicketToMsgConfig/4?leaveType=0

    data:
      leaveType: 0
  extract:
    get_leave_id: $..id
#    get_ticketDeployName: $..ticketDeployName
  export:
    - get_leave_id
#    - get_ticketDeployName

  validate:

    - eq: [ $..retCode,'000000' ]
    - eq: [$..id,'${get_leave_id}' ]
#    - eq: [ $..ticketDeployId, '${get_ticketDeployId}']
#    - eq: [ $..ticketDeployName,'默认插件' ]
#    - eq: [ $.items.0.fieldName,"客户标签"]
#    - eq: [ $..iframeId,'${get_iframeId}']



